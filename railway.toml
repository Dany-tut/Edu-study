[build]
builder = "nixpacks"
buildCommand = "npm rebuild && npx prisma generate && npm run build"

[deploy]
startCommand = "node server/index.js"
healthcheckPath = "/api/health"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
