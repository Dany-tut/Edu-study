-- Вопросы диагностики, которые не укладываются в «выбор из четырёх».
--
-- Первый такой тип — `term`: таблица с ячейкой «?», в которую вписывается
-- термин (Линия 1 ЕГЭ по биологии). Таблица, картинки в ячейках и список
-- принимаемых ответов лежат в payload; у обычных вопросов payload пустой, и
-- options/correct работают как раньше.
alter table public.diag_questions
  add column if not exists payload jsonb;
