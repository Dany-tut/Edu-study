#!/bin/sh
# Каждый коммит несёт свой номер сборки: public/version.json штампуется и
# уезжает внутри этого же коммита. Так номер в профиле приложения = номер
# коммита, и видно, доехала ли обнова до конкретного устройства.
node scripts/stampVersion.mjs --next --stage || exit 0
